import React, { Component } from 'react';

/**
  * Index that displays all mages. Button will be on main page (current
      placeholder text to show where button will be rendered in main page).
*/
class Mages extends Component {
  render() {
    return (
      <div>Button to Mages</div>
    );
  }
}

export default Mages;
