import React, { Component } from 'react';

/**
  * Displays all mages. The button to navigate to this page will be on main page.
  * The current text in the <div> element is a placeholder, so developers can
  * see where the button will be rendered on the main page.
*/
class Mages extends Component {
  render() {
    return (
      <div>Button to Mages</div>
    );
  }
}

export default Mages;
